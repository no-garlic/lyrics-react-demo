import React from 'react';
import ReactDOM from 'react-dom/client';
import LyricStream from './components/LyricStream.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <LyricStream />
  </React.StrictMode>
);
